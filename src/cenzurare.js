const sampleText = "javascript este minunat";
const dictionary = ["este"];

const cenzurareText = (text, dictionary) => {
  const candidate = text.replace();
};
console.log(cenzurareText(sampleText, dictionary));
